# 2026 年 2 月提交汇总

## 统计信息

- **统计周期**: 2026-02-01 至 2026-02-28
- **涉及仓库数**: 5 个
- **总提交数**: 207+ 条

## 仓库列表

1. **aigc-chatbot** - 后端服务
2. **aigc-chatbot-web** - Web 前端应用
3. **aigc-prompt-web** - 提示词管理
4. **store-health-web** - 门店健康度
5. **ai-design** - AI 设计

---

## 按仓库分类

### 1. aigc-chatbot (后端服务)

#### 主要功能特性

- 集成 SenseVoice 语音识别引擎，升级 Whisper 至 medium 模型
- Socket.IO 支持多命名空间，兼容移动端连接
- 支持 AMR 音频格式上传
- AdminConfig 配置优化（WEBUI_URL 改为可选）
- 权限组权限合并逻辑修复
- 任务模板自动补全功能完善
- 依赖管理迁移至 uv

#### 关键修复

- 修复 SenseVoice 模型加载导致 Worker OOM 的问题
- 修复音频转写接口因缺少 FFmpeg 导致请求卡死
- 修复 SenseVoice 因容器内网络不通导致模型加载失败
- 修复权限组权限合并逻辑
- 修复流水线部署因 lxml 下载超时导致构建失败
- 修复日志记录 500 错误
- 修复图像生成参数验证失败

#### 构建优化

- 迁移至 uv 依赖管理并重构 Dockerfile 分层缓存策略
- 合并 RUN 指令减少镜像层数
- 统一 Python 和系统包国内源配置

---

### 2. aigc-chatbot-web (Web 前端应用)

#### 主要功能特性

- **版本管理**: 实现应用版本检查和自动热更新机制
- **消息输入**: 展示选中的提示词引用及其内容预览
- **图像生成**: 新建聊天时初始化图像生成配置
- **ToolsMenu 组件**: 新增组件完善功能选择显示
- **PAPP 适配**: 适配 JW SDK 原生录音能力
- **录音功能**: 实现 PAPP 录音文件上传和下载

#### 移动端优化

- 修复 Android WebView 文件下载失败
- 修复 Android 键盘弹出后抽屉高度错误
- 优化 MessageInput 功能指示器
- 优化响应式布局（TreeSelect、模型选择器等）
- 侧边栏宽度优化为屏幕的 75-85%
- 隐藏移动设备上的文本标签和菜单按钮文本

#### 关键修复

- 修复 PAPP 环境 WebSocket 连接失败和白屏问题
- 修复 PAPP 路由跳转缺失 basePath 的问题
- 修复版本不匹配处理方式
- 修复 JoyWork SDK 重复初始化
- 修复 Modal 组件 reactive statement 依赖问题
- 修复 Thinking 折叠框自动关闭问题
- 修复引文列表重复显示问题

#### 调试与工具

- 移除 vconsole，替换为 eruda 调试工具
- 注入 git hash 和构建时间标识
- 增强 Socket 连接和音频处理调试日志
- 添加构建版本验证脚本

#### 文件下载优化

- 优化文件下载工具
- 新增 iframe 方式支持 Android WebView
- 调整文件下载降级策略
- 增强各环境下的可观测性和兼容性

#### 组件重构

- 提取 RangeSlider 组件统一滑块实现
- 提取 ToolsMenu 组件简化新建对话按钮逻辑
- 提取手势识别逻辑为通用 action
- 移除引文组件卡片式布局，恢复原始简洁样式
- 统一新对话按钮逻辑

#### 样式改进

- 优化 MessageInput 功能指示器样式
- 统一功能指示器文本大小至 13px
- 优化嵌入层批处理输入区域布局
- 增大移动端侧边栏用户按钮点击区域

#### 其他优化

- 简化响应自动复制功能
- 优化 JoyWork 授权码获取流程
- 简化 PAPP 录音上传 - 下载流程
- 优化管理后台表格移动端滚动体验

---

### 3. aigc-prompt-web (提示词管理)

#### 主要更新

- 增强 API 调用示例展示，分离组装接口和原始信息
- 添加 settings.local.json 用于目录权限配置

---

### 4. store-health-web (门店健康度)

#### 主要更新

- 指标卡片接口改为按 primaryMetricName 并发调用
- 调整首页指标卡片排列顺序，将销售和 SPC 指标前置
- 更新安全元数据文件，添加 FGID 和标签信息

---

### 5. ai-design (AI 设计)

#### 主要功能特性

- 修改为 Grsai 的图像生成 API
- 添加多参考图功能
- 优化包装设计提示词让 Slogan 元素自然融入

#### 关键修复

- 修复历史记录中参考图和 Slogan 图片无法显示
- 修复 TypeScript 类型错误

---

## 提交类型统计

### 功能提交 (feat)

- 版本检查和自动热更新
- SenseVoice 语音识别引擎集成
- ToolsMenu 组件
- PAPP 环境 JW SDK 原生录音
- Socket.IO 多命名空间支持
- 多参考图功能
- 多个消息输入和功能显示优化

### 修复提交 (fix)

- 超过 50+ 个 bug 修复
- 主要涵盖：PAPP 适配、WebSocket 连接、文件下载、权限处理等

### 重构提交 (refactor)

- 15+ 项组件和功能重构
- 包括：RangeSlider、ToolsMenu、手势识别逻辑提取等

### 样式提交 (style)

- 15+ 项样式优化
- 主要为消息输入、功能指示器、响应式布局优化

### 其他提交 (chore/build/docs)

- 依赖管理迁移至 uv
- Docker 构建优化
- 调试日志和日志记录
- README 文档重构

---

## 关键收获

### 技术栈升级

- ✅ 依赖管理迁移至 uv
- ✅ 语音识别引擎升级（Whisper → SenseVoice）
- ✅ 调试工具升级（vconsole → eruda）

### 平台适配

- ✅ PAPP 移动环境深度适配
- ✅ Android WebView 兼容性全面改进
- ✅ JW SDK 集成和优化

### 产品体验

- ✅ 应用热更新机制实现
- ✅ 消息输入功能完善
- ✅ 移动端响应式优化

### 代码质量

- ✅ 大量组件重构和逻辑提取
- ✅ 错误处理和防御性编程
- ✅ 调试和监控日志完善
